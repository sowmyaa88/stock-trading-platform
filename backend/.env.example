PORT=3002
MONGO_URL=mongodb+srv://<username>:<password>@cluster0.mongodb.net/stock-trading-platform?retryWrites=true&w=majority

# SECURITY: Generate a strong 32+ byte random secret for JWT signing in .env (e.g. `openssl rand -hex 32`)
JWT_SECRET=generate_a_random_32_byte_secret_key_here_for_production
